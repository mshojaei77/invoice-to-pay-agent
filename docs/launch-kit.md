# Launch Kit

## Repository Metadata

Description:

```text
Open-source invoice-to-pay agent for AP automation: OCR/VLM extraction, 3-way matching, approvals, fraud checks, ERP mock posting, evals, and audit logs.
```

Website:

```text
https://mshojaei77.github.io/invoice-to-pay-agent
```

Topics:

```text
accounts-payable
invoice-processing
invoice-automation
ap-automation
finance-automation
ai-agent
agentic-ai
langgraph
fastapi
pydantic
document-ai
ocr
intelligent-document-processing
erp
netsuite
po-matching
fraud-detection
human-in-the-loop
llm-evaluation
python
```

## Issues To Open

1. `good first issue: add Streamlit review UI polish`
2. `good first issue: add README architecture diagram refinements`
3. `good first issue: add PostgreSQL persistence for runs`
4. `good first issue: add MinIO document archive`
5. `good first issue: add CSV export for ERP posting payload`
6. `good first issue: add sample invoice eval case`
7. `help wanted: add QuickBooks mock connector`
8. `help wanted: add NetSuite vendor bill draft adapter`

Use the `good first issue` label for 1-6 and `help wanted` for 7-8.

## Warm DM

```text
Hey - I built a small open-source invoice-to-pay agent.

Not just OCR: it models PO matching, duplicate checks, approval routing, fraud controls, ERP mock posting, and audit logs as a LangGraph workflow.

Would love your blunt feedback:
https://github.com/mshojaei77/invoice-to-pay-agent
```

## LinkedIn Launch

```text
Most invoice AI demos stop at OCR.

But AP teams do not lose most time extracting text.
They lose it on missing POs, duplicate invoices, vendor mismatches, GL coding, approval chasing, payment holds, and audit reconstruction.

So I built a small open-source Invoice-to-Pay Agent that models the actual AP workflow as a typed LangGraph graph.

It handles:
- parser routing
- invoice / PO / delivery matching
- duplicate checks
- exception classification
- fraud controls
- approval routing
- ERP mock posting
- audit logs
- eval scenarios

Repo:
https://github.com/mshojaei77/invoice-to-pay-agent

I am looking for feedback from finance automation builders, AP people, ERP consultants, and AI engineers.
```

## Hacker News

Title:

```text
Show HN: Invoice-to-Pay Agent - LangGraph workflow for AP automation
```

Body:

```text
I built a small open-source prototype for invoice-to-pay automation.

Most invoice demos stop at OCR. This one models the AP workflow after extraction: PO/receipt matching, duplicate checks, fraud controls, approval routing, payment holds, ERP mock posting, and audit logs.

It is built with FastAPI, LangGraph, Pydantic v2, parser routing, pytest scenarios, and sample PDFs.

I would love feedback from people who have worked with AP, ERP integrations, document AI, or finance automation.
```

## Reddit Feedback Post

```text
I built an open-source AP automation prototype and want feedback from people who process invoices.

It is not a product pitch. I am trying to understand whether the workflow logic matches real AP pain:
- PO matching
- duplicate checks
- approval routing
- payment holds
- vendor mismatch
- ERP handoff
- audit log

What am I missing?
```
